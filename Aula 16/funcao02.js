function soma(n1=0, n2=0) { //a função pré determina que se um dos dois valores não for recebido então vão ser 0
    return n1 + n2
}

console.log(`A soma dos números é ${soma(2)}`) //somo dois valores e exibe na tela